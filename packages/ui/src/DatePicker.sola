<script>
  export let label = "DATE PICKER";
  export let initialValue = "";
  export let placeholder = "Pick a date...";
  let value = $state(initialValue);
</script>

<div class="sola-datepicker-root">
  {#if label}
    <label class="sola-label">{label}</label>
  {/if}
  <div class="sola-picker-input">
    <input type="date" class="sola-input-native" bind:value="value" placeholder={placeholder} />
  </div>
</div>

<style>
  .sola-datepicker-root { display: flex; flex-direction: column; gap: 6px; width: 100%; }
  .sola-label { font-size: 11px; font-weight: 700; color: var(--sola-color-neutral-500, #64748b); text-transform: uppercase; font-family: monospace; }
  .sola-picker-input { position: relative; width: 100%; }
  .sola-input-native {
    width: 100%;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 10px 14px;
    color: var(--sola-color-neutral-0, #fff);
    font-size: 13px;
    outline: none;
    font-family: inherit;
  }
  .sola-input-native:focus { border-color: var(--sola-color-primary-500, #10b981); box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.15); }
</style>
