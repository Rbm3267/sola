<script>
  export let label = '';
  export let variant = 'default';
</script>

<span class="badge {variant}">{label}</span>

<style>
  .badge {
    display: inline-block;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 4px 10px;
    border-radius: 6px;
  }

  .default {
    background: var(--sola-surface-2, #f1f5f9);
    color: var(--sola-text-secondary, #64748b);
  }

  .success {
    background: var(--sola-alert-positive-bg, #ecfdf5);
    color: var(--sola-alert-positive, #059669);
  }

  .warning {
    background: #fffbeb;
    color: #d97706;
  }

  .error {
    background: var(--sola-alert-high-bg, #fef2f2);
    color: var(--sola-alert-high, #dc2626);
  }
</style>
