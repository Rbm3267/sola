<script>
  let activeSection = $state("quickstart");
</script>

<div class="sola-docs-root">
  <div class="docs-header">
    <div class="header-badge">DOCUMENTATION</div>
    <h1 class="header-title">Sola Intent & Runtime Guide</h1>
    <p class="header-desc">Everything you need to build zero-VDOM apps with native .sola single-file components.</p>
  </div>

  <div class="docs-grid">
    <div class="docs-section-card">
      <h3>1. What is .sola?</h3>
      <p>A single-file component architecture combining reactive signals, scoped styles, and declarative HTML templates compiled directly to O(1) DOM updates.</p>
    </div>
    <div class="docs-section-card">
      <h3>2. Zero-VDOM Signals</h3>
      <p>Use <code>$state(val)</code> and <code>$derived(() => expr)</code> to bind values straight to text nodes without virtual DOM diffing.</p>
    </div>
    <div class="docs-section-card">
      <h3>3. Multi-Framework Export</h3>
      <p>Write once in .sola and export directly to React 19 JSX, Svelte 5 runes, or standard Web Components.</p>
    </div>
  </div>
</div>

<style>
  .sola-docs-root {
    width: 100%;
    max-width: 1000px;
    margin: 0 auto;
    padding: 40px 24px;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    color: #f8fafc;
  }
  .docs-header { text-align: center; margin-bottom: 40px; }
  .header-badge {
    display: inline-block;
    font-family: monospace;
    font-size: 11px;
    font-weight: 700;
    color: #38bdf8;
    background: rgba(56, 189, 248, 0.1);
    padding: 4px 10px;
    border-radius: 9999px;
    margin-bottom: 12px;
  }
  .header-title { font-size: 34px; font-weight: 900; color: #fff; margin-bottom: 8px; }
  .header-desc { font-size: 15px; color: #94a3b8; }
  .docs-grid { display: flex; flex-direction: column; gap: 16px; }
  .docs-section-card {
    background: rgba(15, 23, 42, 0.9);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 18px;
    padding: 24px;
  }
  .docs-section-card h3 { font-size: 17px; font-weight: 800; color: #fff; margin-top: 0; margin-bottom: 8px; }
  .docs-section-card p { font-size: 13px; color: #94a3b8; line-height: 1.6; margin: 0; }
  .docs-section-card code {
    background: rgba(0, 0, 0, 0.4);
    color: #38bdf8;
    padding: 2px 6px;
    border-radius: 4px;
    font-family: monospace;
    font-size: 12px;
  }
</style>
