<script>
  let prompt = $state("Show Q3 enterprise revenue and churn rate");
  let status = $state("Idle");
  let count = $state(42);
</script>

<div class="sola-native-sandbox">
  <div class="sandbox-header">
    <div class="window-controls">
      <span class="dot dot-red"></span>
      <span class="dot dot-amber"></span>
      <span class="dot dot-green"></span>
    </div>
    <div class="window-title">Native Sola Component Runtime (.sola)</div>
    <div class="engine-badge">Zero-DOM Compiler</div>
  </div>

  <div class="sandbox-body">
    <div class="prompt-box">
      <div class="spark-icon">✦</div>
      <input 
        type="text" 
        bind:value="prompt" 
        class="prompt-input"
        placeholder="Type an intent for Sola runtime..."
      />
      <button class="resolve-btn">
        <span>$intent</span>
      </button>
    </div>

    <div class="metrics-grid">
      <div class="metric-card">
        <div class="metric-label">Reactive Signals</div>
        <div class="metric-val">{count()}</div>
        <div class="metric-footer">
          <button on:click={() => count.set(count() + 1)} class="pill-btn">+ Increment Signal</button>
        </div>
      </div>

      <div class="metric-card">
        <div class="metric-label">Runtime Engine</div>
        <div class="metric-val">3.2 kB</div>
        <div class="metric-footer">
          <span class="tag-success">Zero VDOM Overhead</span>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .sola-native-sandbox {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(24px);
    border: 1px solid rgba(226, 232, 240, 0.9);
    border-radius: 24px;
    box-shadow: 0 20px 60px -15px rgba(15, 23, 42, 0.08);
    overflow: hidden;
    margin-top: 24px;
  }

  .sandbox-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 14px 20px;
    background: rgba(248, 250, 252, 0.8);
    border-bottom: 1px solid rgba(241, 245, 249, 1);
  }

  .window-controls {
    display: flex;
    gap: 6px;
  }

  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .dot-red { background: #fca5a5; }
  .dot-amber { background: #fcd34d; }
  .dot-green { background: #86efac; }

  .window-title {
    font-size: 12px;
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-weight: 600;
    color: #64748b;
  }

  .engine-badge {
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding: 3px 8px;
    border-radius: 6px;
    background: #f0fdf4;
    color: #166534;
    border: 1px solid rgba(34, 197, 94, 0.2);
  }

  .sandbox-body {
    padding: 24px;
  }

  .prompt-box {
    display: flex;
    align-items: center;
    gap: 12px;
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    border-radius: 14px;
    padding: 8px 14px;
    margin-bottom: 20px;
    transition: border-color 0.15s ease;
  }

  .prompt-box:focus-within {
    border-color: #0ea5e9;
    box-shadow: 0 0 0 3px rgba(14, 165, 233, 0.12);
  }

  .spark-icon {
    color: #0ea5e9;
    font-weight: 800;
    font-size: 16px;
  }

  .prompt-input {
    flex: 1;
    border: none;
    background: transparent;
    font-size: 14px;
    font-weight: 500;
    color: #0f172a;
    outline: none;
  }

  .resolve-btn {
    background: #0f172a;
    color: #ffffff;
    font-size: 12px;
    font-family: ui-monospace, SFMono-Regular, monospace;
    font-weight: 700;
    padding: 6px 12px;
    border-radius: 8px;
    border: none;
    cursor: pointer;
    transition: opacity 0.15s ease;
  }

  .resolve-btn:hover {
    opacity: 0.9;
  }

  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 16px;
  }

  .metric-card {
    background: #ffffff;
    border: 1px solid #f1f5f9;
    border-radius: 16px;
    padding: 16px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.02);
  }

  .metric-label {
    font-size: 11px;
    font-weight: 700;
    color: #64748b;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-family: ui-monospace, SFMono-Regular, monospace;
    margin-bottom: 8px;
  }

  .metric-val {
    font-size: 28px;
    font-weight: 900;
    color: #0f172a;
    letter-spacing: -0.02em;
    margin-bottom: 12px;
  }

  .pill-btn {
    background: #f1f5f9;
    color: #334155;
    border: 1px solid #e2e8f0;
    border-radius: 8px;
    padding: 5px 10px;
    font-size: 11px;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.15s ease;
  }

  .pill-btn:hover {
    background: #e2e8f0;
  }

  .tag-success {
    font-size: 11px;
    font-weight: 600;
    color: #10b981;
  }
</style>
